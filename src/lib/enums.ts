export enum RelationshipTypeEnum {
  unit = 'unit',
  aggregate = 'aggregate',
}

export enum DocumentTypeEnum {
  cpf = 'cpf',
  cnpj = 'cnpj',
}

export enum TripSteps {
  one = '1',
  two = '2',
  three = '3',
  four = '4',
  five = '5',
}
