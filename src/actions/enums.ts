export enum RelationshipTypeEnum {
  // Fleet
  unit = 'unit',

  aggregate = 'aggregate',
}

export enum TripSteps {
  // Steps
  one = '1',
  two = '2',
  three = '3',

  // Draft steps
  four = '4',
  five = '5',
}
